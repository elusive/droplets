import { Routes } from '@angular/router';
import { DropletsComponent } from '@src/app/features/droplets/pages/droplets-main/droplets-main.component';

export const routes: Routes = [{ path: '', component: DropletsComponent }];
