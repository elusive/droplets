import { Routes } from '@angular/router';
import { DropletsComponent } from './features/droplets/pages/droplets-main.component';

export const routes: Routes = [{ path: '', component: DropletsComponent }];
